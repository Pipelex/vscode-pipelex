name = "feature_structured_output_delivery_round"
description = "A driver's round sheet filed into stops, each with its parcels and any access note"

validity = "valid"
tier = "dry"
granularity = "composite"

covers = [
  "feature.structured_output",
]
