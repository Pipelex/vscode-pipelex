# The MTHDS Test Corpus tag vocabulary — the closed set an entry's `covers` draws from.
#
# GENERATED by `pipelex-dev generate-corpus-vocabulary`. Do not edit this file: `native.*`,
# `operator.*`, `controller.*` and `error.*` are derived from the runtime's own registries,
# `feature.*` is hand-maintained in the generator because no registry describes a language feature,
# and every exclusion reason is declared in the generator's source alongside them.
#
# An `error.*` tag that is not excluded also carries `fails_at`, the earliest layer of checking that
# rejects a bundle carrying the fault: `schema` when a check of the document's shape alone already
# catches it, `runtime` when the document has to be interpreted to notice. A structural consumer
# expects a diagnostic on exactly the `schema` entries and none on the others.
#
# This file ships in the pipelex wheel, so it names a document its readers can reach:
# https://docs.pipelex.com/contribute/mthds-test-corpus/ — which in turn points at the
# cross-repo contract, `docs/specs/mthds-test-corpus.md` in the Pipelex workspace repo.

[native.dynamic]
code = "Dynamic"

[native.text]
code = "Text"

[native.image]
code = "Image"

[native.document]
code = "Document"

[native.html]
code = "Html"

[native.text_and_images]
code = "TextAndImages"

[native.number]
code = "Number"

[native.yes_no]
code = "YesNo"

[native.date]
code = "Date"

[native.time]
code = "Time"

[native.page]
code = "Page"

[native.json]
code = "JSON"

[native.search_result]
code = "SearchResult"

[native.anything]
code = "Anything"

[native.composite]
code = "Composite"

[operator.func]
code = "PipeFunc"
excluded = "A PipeFunc names a Python function that is resolved against the runtime's function registry at validation time, so an entry declaring one fails with \"Function '<name>' not found in registry\" in every consumer that has not registered that exact function. A corpus entry has to validate everywhere, and the cross-language consumers cannot register a Python function at all, so no portable focused entry exists. Exercise PipeFunc in a consumer's own fixtures."

[operator.img_gen]
code = "PipeImgGen"

[operator.compose]
code = "PipeCompose"

[operator.llm]
code = "PipeLLM"

[operator.extract]
code = "PipeExtract"

[operator.search]
code = "PipeSearch"

[operator.structure]
code = "PipeStructure"

[controller.batch]
code = "PipeBatch"

[controller.condition]
code = "PipeCondition"

[controller.parallel]
code = "PipeParallel"

[controller.sequence]
code = "PipeSequence"

[error.missing_input_variable]
code = "missing_input_variable"
fails_at = "runtime"

[error.extraneous_input_variable]
code = "extraneous_input_variable"
fails_at = "runtime"

[error.input_stuff_spec_mismatch]
code = "input_stuff_spec_mismatch"
fails_at = "runtime"

[error.inadequate_output_concept]
code = "inadequate_output_concept"
fails_at = "runtime"

[error.inadequate_output_multiplicity]
code = "inadequate_output_multiplicity"
fails_at = "runtime"

[error.circular_dependency_error]
code = "circular_dependency_error"
excluded = "Raised only from the pipe sorter, and the pipe sorter runs only on `PipelexBundleSpec.to_blueprint()` — the builder's spec-to-blueprint conversion, which is how an AI-authored spec becomes a bundle. A `.mthds` file parses straight into a blueprint and never passes through it, so no corpus entry can produce this. Measured rather than read off the call graph: a self-referencing PipeSequence fails the dry run with `DryRunError`, and a mutually recursive PipeCondition pair exhausts the interpreter's stack — neither reports a circular dependency."

[error.llm_output_cannot_be_image]
code = "llm_output_cannot_be_image"
fails_at = "runtime"

[error.invalid_pipe_code_syntax]
code = "invalid_pipe_code_syntax"
fails_at = "runtime"

[error.unknown_pipe_type]
code = "unknown_pipe_type"
fails_at = "schema"

[error.missing_pipe_type]
code = "missing_pipe_type"
fails_at = "schema"

[error.batch_item_name_collision]
code = "batch_item_name_collision"
fails_at = "runtime"

[error.optional_marker_invalid]
code = "optional_marker_invalid"
fails_at = "runtime"

[error.optional_not_handled]
code = "optional_not_handled"
fails_at = "runtime"

[error.optional_output_required]
code = "optional_output_required"
fails_at = "runtime"

[error.optional_input_unguarded]
code = "optional_input_unguarded"
fails_at = "runtime"

[error.optional_branch_required_field]
code = "optional_branch_required_field"
fails_at = "runtime"

[error.optional_force_redundant]
code = "optional_force_redundant"
excluded = "Advisory-only: it rides the validation report's `warnings` array and never makes a verdict invalid. An entry contract that models one `expected_error` on an invalid entry has nowhere to put a warning on a valid one, so there is no shape a focused entry could take. Revisit if the manifest ever grows a warnings axis."

[error.native_concept_redeclaration]
code = "native_concept_redeclaration"
fails_at = "runtime"

[error.unresolved_concept]
code = "unresolved_concept"
fails_at = "runtime"

[error.unresolved_pipe_dependency]
code = "unresolved_pipe_dependency"
fails_at = "runtime"

[error.unknown_validation_error]
code = "unknown_validation_error"
excluded = "The generic fallback for a validation failure that matched no specific code. Reaching it on purpose would mean authoring a bundle that breaks in a way the categorizer does not recognize — which is a state to fix in the runtime, not to pin in a corpus entry that would then have to be rewritten the moment the fault gets a code of its own."

[error.unknown_concept]
code = "unknown_concept"
excluded = "Shadowed: it fires in the pipe factory when a pipe's output names a concept that is neither native, nor declared in the pipe's own domain, nor domain-qualified — exactly the condition the earlier concept-reference resolution already rejects, and it reports the same fault as `unresolved_concept`. Measured across unqualified, cross-domain and qualified-but-missing output refs: every one of them comes back as `unresolved_concept`, so the factory's own check is unreachable from a `.mthds` file."

[error.unknown_factory_error]
code = "unknown_factory_error"
excluded = "The generic fallback for a pipe-construction failure that matched no specific code, and excluded for the same reason as its validation counterpart: an entry pinning it would be pinning the absence of a diagnosis rather than a language fault."

[error.dry_run_error]
code = "DryRunError"
fails_at = "runtime"

[feature.multi_file_library]
description = "A method split across several `.mthds` files in one directory and merged additively behind a `bundle.mthds` entry point, so a pipe can be forward-declared as a signature in one file and satisfied by a concrete definition in another."

[feature.optionals]
description = "An input declared optional with the `?` suffix, so a run that has no value for it proceeds instead of failing and the absence is recorded in the working memory. How a method copes with the absence is the method's business — guarding the reference in a prompt, in a condition expression, or letting it lift through a chain."

[feature.smart_inputs]
description = "Bare caller values — a string, a number, a dict, a list — interpreted top-down against the entry pipe's declared signature, so a caller need not wrap each one in its concept envelope."

[feature.structured_output]
description = "A concept declaring a `structure` table of typed fields, and a pipe producing that shape rather than free text, so the runtime validates the output against the declared fields."
